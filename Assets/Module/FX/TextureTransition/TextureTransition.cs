using UnityEngine;

public class TextureTransition : MonoBehaviour
{
    [SerializeField]
    ComputeShader _textureTransitionCs;

    public Texture2D PlayTransition(Texture2D from, Texture2D to, float durationPerFrame)
    {
        
        return to;
    }
}
